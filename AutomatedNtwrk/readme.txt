    The Bash script should be run on a system with telnet access to the router or switch.
    The PowerShell script should be executed on the Windows machine that needs network configuration.
    The Python script requires Python installed with the netmiko library (pip install netmiko).